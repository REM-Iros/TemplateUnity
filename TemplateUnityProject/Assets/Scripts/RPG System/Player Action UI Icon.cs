using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class PlayerActionUIIcon : MonoBehaviour
{
    // Action texts, icons, etc attached to the player
    private TextMeshProUGUI _actionText;
    private Image _actionControlIcon;
    private Image _actionAttackIcon;

    /// <summary>
    /// Sets the text
    /// </summary>
    /// <param name="text"></param>
    /// <param name="controlIcon"></param>
    /// <param name="attackIcon"></param>
    public void Init(string text, Image controlIcon, Image attackIcon)
    {

    }
}
