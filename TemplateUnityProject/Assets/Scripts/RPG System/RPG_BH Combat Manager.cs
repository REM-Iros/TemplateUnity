using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This script will be the combat controller for the rpg bullet hell segments.
/// It should orchestrate attacks, abilities, and enemy interactions. This is not
/// a monolithic script, but rather a manager that will call other scripts and systems.
/// 
/// REM-i
/// </summary>
public class RPG_BHCombatManager : MonoBehaviour
{
    #region Vars

    private PlayerTeamController _playerTeamController;

    #endregion

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
